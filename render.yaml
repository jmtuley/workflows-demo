services:
  - type: workflow
    name: demo-workflow
    runtime: node
    region: oregon
    branch: master
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: LOG_LEVEL
        value: info
      - fromGroup: demo-shared
      - key: CH_CH_CHANGES
        value: stay-the-same

envVarGroups:
  - name: demo-shared
    envVars:
      - key: FEATURE_FLAG
        value: "true"
