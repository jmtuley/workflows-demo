# C1: startCommand omitted. Expect a required-field error on
# services[0].startCommand. A workflow has no runtime default to fall back on.
services:
  - type: workflow
    name: wf-nostart
    runtime: node
    region: oregon
    buildCommand: npm install
