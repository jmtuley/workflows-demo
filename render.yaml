# D2: expect "changing region not supported". The rest of the block is
# unchanged from 10 so the region is the only candidate diff.
services:
  - type: workflow
    name: wf-minimal
    runtime: node
    region: frankfurt
    buildCommand: npm ci
    startCommand: npm run worker
    rootDir: src
    autoDeploy: "off"
