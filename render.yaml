# F3 (repo 1): same block, so Blueprint 1 can win the race first.


services:
  - type: workflow
    name: wf-race
    runtime: node
    repo: https://github.com/jmtuley/workflows-demo
    branch: master
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: commit
