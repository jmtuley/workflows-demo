# E2: must match the CLI-created wf-adopted exactly, so the only action is the
# capture itself. Adjust these values to whatever `render workflows create`
# actually produced before syncing.
services:
  - type: workflow
    name: wf-adopted
    runtime: node
    region: oregon
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: commit
