# G2: prod -> staging, same project.
projects:
  - name: wf-project
    environments:
      - name: prod
        services: []
      - name: staging
        services:
          - type: workflow
            name: wf-minimal
            runtime: node
            region: oregon
            startCommand: npm start
