# I7: WF-Renamed declared against an existing wf-renamed. Names are not
# normalised, so this probably creates a SECOND workflow rather than matching.
services:
  - type: workflow
    name: WF-Renamed
    runtime: node
    region: OREGON
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: commit
