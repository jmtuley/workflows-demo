# D1: same workflow as 01, four fields changed. Expect one "Update workflow
# wf-minimal" line, not a create.
services:
  - type: workflow
    name: wf-minimal
    runtime: node
    region: oregon
    buildCommand: npm ci
    startCommand: npm run worker
    rootDir: src
    autoDeploy: "off"
