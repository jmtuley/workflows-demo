# C5: autoDeploy outside {commit, off, checksPass}. Only reached on the UPDATE
# path, so wf-minimal must already exist.
services:
  - type: workflow
    name: wf-autodeploy-new
    runtime: node
    region: oregon
    startCommand: npm start
    autoDeploy: sometimes
