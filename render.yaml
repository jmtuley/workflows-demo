# G1: move an ungrouped workflow into wf-project / prod. Expect an environment
# move action batched with any other resource heading to the same environment.
projects:
  - name: wf-project
    environments:
      - name: prod
        services:
          - type: workflow
            name: wf-minimal
            runtime: node
            region: oregon
            startCommand: npm start
